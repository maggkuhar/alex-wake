# Продажи

Blueprint `sales`. Корзина, оформление заказа, сообщения/диалоги.

## Маршруты
- `/cart` — корзина
- `/cart/add` — добавить в корзину
- `/cart/remove` — убрать из корзины
- `/checkout` — оформление заказа
- `/messages` — список диалогов
- `/messages/<id>` — диалог с пользователем
