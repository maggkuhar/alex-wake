# Блок: Alex Wake (галерея)

*Проект:* Alice Bisell
*Папка:* `/root/alicebisell/alexwake`
*Порт:* 5002 · *Сервис:* `alexwake.service` · *GitHub:* `maggkuhar/alex-wake`
*Поддомен:* `alexwake.interviewwithsubconscious.com`

## Что делает блок

Онлайн-галерея картин (выставка-продажа). Художники регистрируются,
загружают работы; покупатели смотрят каталог, корзина, оплата, чат с
продавцом. Плюс аукционы, печать репродукций, AR-примерка на стену.

## Архитектура / файлы

- Flask + blueprints (один под-блок = одноимённая папка), `app.py` только
  склейка. БД `alexwake.db` (SQLite). Загрузки — `uploads/paintings/`.
- Под-блоки: `галерея/`, `кабинет/`, `продажи/`, `депонирование/`.
- Свой git-репозиторий (раздельно от Эстрады).

## Ключевые решения

- Блочная архитектура — как `blogery` (blueprints в одноимённых папках).
- **Депонирование — отдельный под-блок** (юр. регистрация авторства через
  ЕДРИД), изолировано от торговли.
- БД SQLite, загрузки на диск (не S3) — простота важнее на этом этапе.
- Секреты в `.env` — тех.долг (задача №32, перенос в vault).

## Полное описание

Ядро: `/root/yadro/проекты/alice-bisell/` и `проекты/alexwake/` (если есть).
